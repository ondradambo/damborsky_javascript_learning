// zkouším tento soubor upravit a následně znovu pushnout, tentokrát i včetně přílohy .js

add(565, 584);
subtract(474, 520);
multiply(5822, 65);
divide(632, 6);

function add(a, b) {
  const result = a + b;
  console.log("Výsledek sečtení: " + result);
}

function subtract(a, b) {
  const result = a - b;
  console.log("Výsledek odečtení: " + result);
}

function multiply(a, b) {
  const result = a * b;
  console.log("Výsledek násobení: " + result);
}

function divide(a, b) {
  const result = a / b;
  console.log("Výsledek dělení: " + result);
}
